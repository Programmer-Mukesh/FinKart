// import { createSelector } from "reselect";

// const selectedItem = (state) => {
//   state.cart;
// };

// export const selectedItemToCart = createSelector([selectedItem], (item) => {
//   return item.cartItems;
// });

// export const selectedLengthOfCart = createSelector([selectedItem], (item) => {
//   return item.length;
// });
